---
- hosts: all
  sudo: no

  pre_tasks:
    - name: Update apt cache if needed.
      apt: update_cache=yes cache_valid_time=3600
      sudo: yes

  roles:
    - passwordless_ssh
    - asterixdb
    - supervisord
